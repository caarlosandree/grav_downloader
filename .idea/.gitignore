# Ignora a pasta de dependências do Node.js
node_modules/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Variáveis de ambiente (se você usar arquivos .env)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Arquivos temporários do sistema ou do editor
.DS_Store       # macOS
Thumbs.db       # Windows
ehthumbs.db     # Windows
ehthumbs_vista.db # Windows

# Diretórios de configurações de editor (ex: VS Code)
.vscode/
.idea/          # JetBrains IDEs

# Diretórios de build/output (se você tiver um processo de build no futuro)
dist/
build/
out/

# Arquivos temporários gerados pelo código (se forem criados na raiz ou em subpastas)
# O seu backend atual parece usar o diretório temp do OS (os.tmpdir()),
# mas se mudar para criar arquivos temporários dentro da pasta do projeto, adicione aqui:
# temp/
# *.zip # Se os arquivos ZIP gerados pelo backend estiverem na pasta do projeto
# *.gsm # Se arquivos GSM temporários estiverem na pasta do projeto
# *.mp3 # Se arquivos MP3 temporários estiverem na pasta do projeto

# Arquivos de lock de pacotes específicos
package-lock.json  # Opcional: algumas equipes preferem versionar, outras não.
yarn.lock          # Opcional: similar ao package-lock.json
pnpm-lock.yaml     # Opcional: para pnpm