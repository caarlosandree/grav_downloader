# Default ignored files
/shelf/
/workspace.xml
# Editor-based HTTP Client requests
/httpRequests/
# Datasource local storage ignored files
/dataSources/
/dataSources.local.xml
# Node.js
node_modules/
dist/
build/
.env
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# Log files
*.log
# Runtime files
*.pid
*.sock
# OS generated files
.DS_Store
Thumbs.db

# Temporary directories created by the backend (if they were inside the project)
# Nossa implementação usa o temp dir do OS (os.tmpdir()), que geralmente não precisa ser ignorado no gitignore
# Mas se você criasse um diretório temp DENTRO da pasta do projeto, deveria ignorá-lo aqui.
# Ex: temp_downloads/
